import Layout from '@/components/template/Layout';

export default function Settings() {
    return (
        <div>
            <Layout titulo="Settings" subtitulo="configurando o sistema">
                Conteúdo do settings
            </Layout>
        </div>
    );
}
